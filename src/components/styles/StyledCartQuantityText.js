import styled from "styled-components";

const StyledCartQuantityText = styled.div`
  font-size: 20px;
  font-weight: 700;
  margin: 0;
  margin-bottom: 8px;
  text-align: right;
`;

export default StyledCartQuantityText;
